import 'package:flutter/material.dart';

class HideMenu extends StatelessWidget {
  const HideMenu({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
